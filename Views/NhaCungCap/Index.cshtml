@model IEnumerable<Homepage.Models.NHACUNGCAP>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    
</head>
<body>
    <br/>
    <h3 class="text-center">Nhà Cung Cấp</h3>
    <p class="btn btn-default" style="margin-left:7%">
        @Html.ActionLink("Thêm mới", "Create")
    </p>
    <br/>
    <table class="table table-bordered table-hover" style="margin-left:7%;margin-right:7%">
        <tr>
            <th>
                Tên nhà cung cấp
            </th>
            <th>Thao tác</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TEN_NHACUNGCAP)
                </td>
                <td>
                    @Html.ActionLink("Sửa", "Edit", new { id = item.ID_NCC }) |
                    @Html.ActionLink("Chi tiết", "Details", new { id = item.ID_NCC })
                </td>
            </tr>
        }

    </table>
</body>
</html>
