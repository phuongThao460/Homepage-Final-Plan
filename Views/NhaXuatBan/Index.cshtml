@model IEnumerable<Homepage.Models.NHAXUATBAN>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                MÃ NHÀ XUẤT BẢN
            </th>
            <th>
                TÊN NHÀ XUẤT BẢN
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_NXB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TEN_NHAXUATBAN)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_NXB }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_NXB }) |
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
