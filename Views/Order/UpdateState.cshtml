@model Homepage.Models.DONHANG
    <style>
        .fakeLable{
            background-color:transparent;
            border-style:none;
            width:fit-content();
        }
    </style>
@using (Html.BeginForm("UpdateState", "Order"))
{
    <td>
        @Html.TextBoxFor(modelItem => modelItem.ID_DONHANG, htmlAttributes: new { @readonly = "readonly",@class="fakeLable" })
    </td>
    <td>
        @Html.DisplayFor(modelItem => modelItem.ID_TTKH)
    </td>
    <td>
        <p>@Model.TRANGTHAIDONHANG.TEN_TRANGTHAI</p>
    </td>
    <td>
        @Html.DisplayFor(modelItem => modelItem.THOIGIAN_DAT)
    </td>
    <td>
        @Html.DisplayFor(modelItem => modelItem.TONGTIEN)
    </td>
    <td>
        @{Html.RenderAction("SelectOrderState", "Order");}
    </td>

    <td>
        <input type="submit" value="Lưu trạng thái" />
    </td>
}

