@model IEnumerable<Homepage.Models.TRANGTHAIDONHANG>

@{
    ViewBag.Title = "DanhSachTrangThai";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DanhSachTrangThai</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_TRANGTHAI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEN_TRANGTHAI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_TRANGTHAI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEN_TRANGTHAI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        </td>
    </tr>
}

</table>
