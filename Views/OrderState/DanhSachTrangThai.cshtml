@model IEnumerable<Homepage.Models.TRANGTHAIDONHANG>

@{
    ViewBag.Title = "DanhSachTrangThai";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh SÁCh Trạng Thái</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            MÃ TRẠNG THAÍ
        </th>
        <th>
           TÊN TRẠNG THÁI
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_TRANGTHAI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEN_TRANGTHAI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID_TRANGTHAI }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID_TRANGTHAI }) |
        </td>
    </tr>
}

</table>
